import React from "react";

export const ThemeConfigurationContext = React.createContext({});
export const ThemeConfigurationProvider = ThemeConfigurationContext.Provider;
export const ThemeConfigurationConsumer = ThemeConfigurationContext.Consumer;